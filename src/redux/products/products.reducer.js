
const productInitalState = {
    loading: false,
    error: false,
    data: [],
};

export const productsReducer = (state = productInitalState, { type, payload }) => {

    switch (type) {

        default: {
            return state
        }
    }


};